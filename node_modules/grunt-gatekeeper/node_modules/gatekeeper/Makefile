TESTS = test/*
REPORTER ?= dot

test: test-unit

test-unit:
	@./node_modules/.bin/mocha \
          --require should \
          --require $(REPORTER) \
          --timeout 2000 \
          --growl \
          $(TESTS)


